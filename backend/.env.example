# Server
PORT=5000

# Local PostgreSQL connection URL
DATABASE_URL=postgresql://postgres:postgres@127.0.0.1:5432/thread

# Frontend origin allowed by CORS (Vite dev server)
CLIENT_URL=http://localhost:5173
